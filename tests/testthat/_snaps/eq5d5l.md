# validation works

    `respondentID` variable ('TEST') not present in input data.

---

    `pain` variable ('TEST') not present in input data.

---

    `respondentID` / `surveyID` combinations must not be duplicated.

---

    pain dimension (column 'pain') must be integerish and bounded by 1 and 5 (inclusive).

